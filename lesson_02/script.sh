#!/usr/bin/env bash

function prnt_info {
    echo "Usage: choose_raid.sh [-r raid_level]"
    echo "  -h, --help          help"
    echo "  -r, --raid          raid level"
    return
}

# clear
prnt_info
